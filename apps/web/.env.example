# ===== Required for Auth =====
JWT_SECRET=change-this-to-a-strong-random-secret-in-production

# ===== Required for AI features =====
GROQ_API_KEY=your-groq-api-key-here

# ===== Database (Vercel Postgres) =====
# Leave empty for local dev (uses JSON file at ./data/db.json)
# On Vercel, this is auto-set when you link a Postgres database
# POSTGRES_URL=postgres://...

# ===== Optional: Frontend API URL override =====
# Defaults to /api (same-origin, for Vercel or built-in API routes)
# Set to http://localhost:4000/api if running separate Express server
# NEXT_PUBLIC_API_URL=/api
